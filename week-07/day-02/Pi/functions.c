#include "defines.h"

float circle_area(float radius){
    float area = radius * radius * PI;
    return area;
}

float circumference_func(float radius){
    float circumference = 2 * radius * PI;
    return circumference;
}

